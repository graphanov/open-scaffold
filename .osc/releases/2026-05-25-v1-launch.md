# Release / Evidence Note: v1.0.0 release candidate

## Summary

Prepared the repository-side Open Scaffold v1.0.0 release candidate. The slice bumps the package candidate to `1.0.0`, adds a stability contract, adds a curated changelog, adds a landing page, updates README/roadmap/agent entry points, and records the owner gates that must happen before v1.0.0 is live on npm or GitHub Releases.

This note does not claim live publication. Merge, npm publication, GitHub Release latest movement, website deployment, and any launch announcement remain owner-gated external actions.

## Traceability

- Roadmap / issue / task: repo backlog plan `069-v1-launch`.
- Plan: `.osc/plans/done/069-v1-launch.md`.
- Amendment: `.osc/plans/done/069-v1-launch-amendment-1.md` scopes automation to release-candidate preparation while keeping publication owner-gated.
- Run ID / run packet: N/A — implemented directly from the selected backlog plan.
- Selected source: `backlog_plan`.
- Branch / PR: `feat/069-v1-launch`; https://github.com/graphanov/open-scaffold/pull/113.

## Verification

- `git diff --check`.
- `./verify.sh --strict`.
- `npm test`.
- `npm run build`.
- `npm pack --dry-run --json`.
- `npm publish --dry-run` was attempted and blocked by the repository guard because npm publication requires explicit owner approval, even for publish-shaped commands.
- Local tarball smoke for `open-scaffold-1.0.0.tgz`: `npm exec --yes --package <tarball> -- open-scaffold init --tier min --target <temp>` and standard-tier equivalent both generated scaffolds; downstream `./verify.sh --standard` correctly stopped at the expected "mission not defined" gate until a project mission is filled in.
- Landing page static sanity: `docs/index.html` contains the problem, audience, and first command.

## Outcome

The repository release-candidate artifacts are ready for PR review. Stable surfaces are documented separately from experimental and future surfaces. Live-publication checks from the original plan are intentionally converted to owner-gated post-merge checks by amendment because the runner must not merge, publish, move GitHub Release latest, deploy, or announce.

## Follow-up

- Owner gate: merge the release-candidate PR.
- Owner gate: publish `open-scaffold@1.0.0` to npm after merge.
- Owner gate: verify `npx open-scaffold@1.0.0 init --tier min|standard` from the registry after publication.
- Owner gate: create or mark the `v1.0.0` GitHub Release as **Latest**.
- Owner decision: choose whether and where to announce the launch.
