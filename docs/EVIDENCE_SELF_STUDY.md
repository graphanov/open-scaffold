# Open Scaffold methodology evidence — self-study

Computed at: 2026-05-28T23:02:55.934Z
Commit range: 8be590907210 through HEAD (208 commits, inclusive)
Since filter: none (full history)
Schema: open-scaffold.study.v1

This file is regenerated by `osc study`; do not hand-edit. It reports source-labeled,
observational signals derived from this repository's own committed git history and `.osc/`
artifacts. See `docs/EVIDENCE_METHODOLOGY.md` for the protocol and `docs/AB_COMPARISON_PROTOCOL.md`
for the controlled comparison that this observational study deliberately does not run.

How to read the source labels: `metrics` = composed from `osc metrics`; `osc-artifact` = read
directly from `.osc/` files; `git` = derived from commit history; `unavailable` = the tool cannot
compute this from committed artifacts (it is not zero). `null` means the computation ran but had no
data (for example, no plans are done yet).

## Headline observations

- Coverage (null observation): 3 of 6 value hypotheses have an observable proxy in committed artifacts; 3 are unavailable (H-resume-time, H-context-reexplanation, H-cost) and are reported as null rather than estimated.
- Negative finding: 110 of 112 done plans carry no machine-readable approval decision in their evidence note — an honesty gap in this repository's own records, not evidence of protocol adherence.
- Traceability floor: only 3 commit subject(s) name a known plan slug. This is a floor, not a total — commits that omit the slug are uncounted, so the figure understates real plan-to-commit traceability.

## D-activity — descriptive

FAQ: "(no FAQ value claim — descriptive baseline)"

Claim: Repository activity baseline that gives the value signals context. These are descriptive facts, not evidence for any FAQ claim.

| Signal | Value | Source |
|---|---|---|
| Plans in scope | 125 count | metrics |
| Plans reaching done | 112 count | metrics |
| Commits in range | 208 count | git |
| Median cycle time | 0.12 days | metrics |
| Mean cycle time | 1.33 days | metrics |

- Plans in scope: Committed plan files in scope; the denominator the other ratios are read against.
- Plans reaching done: Plans that have moved into done/.
- Commits in range: Commits in the studied range; activity context, not a value claim.
- Median cycle time: Median days from plan creation to close; null when no plans are done yet. Descriptive only — the FAQ makes no cycle-time promise.
- Mean cycle time: Mean days from plan creation to close; null when no plans are done yet.

## H-scope-drift — observable

FAQ: "How much time does this actually save me vs. just winging it?"

Claim: Adopting the scaffold yields "fewer 'wait, I thought we decided X' moments" — less silent scope drift.

| Signal | Value | Source |
|---|---|---|
| Amendment files (total) | 20 count | osc-artifact |
| Plans with >=1 amendment | 16 count | osc-artifact |
| Amendments per plan | 0.16 ratio | osc-artifact |

- Amendment files (total): Count of -amendment-N.md files across in-scope plans. Scope changes captured as amendments rather than as silent edits; a count alone cannot tell whether low numbers mean little drift or uncaptured drift.
- Plans with >=1 amendment: Distinct in-scope plans carrying at least one amendment.
- Amendments per plan: amendment_total / total_plans; null when no plans are in scope.

## H-reviewer-reconstruction — observable

FAQ: "Is this useful for consulting, client delivery, or compliance/audit work?"

Claim: A reader can answer "what did you actually do, why, and how do you know it worked?" months later without reconstructing a chat log.

| Signal | Value | Source |
|---|---|---|
| Evidence completeness | 93.8 percent | metrics |
| Done plans with evidence notes | 105 count | metrics |
| Commits naming a plan slug | 3 count | git |

- Evidence completeness: Share of done plans that have an evidence/release note; null when no plans are done yet.
- Done plans with evidence notes: Done plans with at least one evidence/release note a reviewer could read.
- Commits naming a plan slug: Commits whose subject names a known plan slug. A floor on plan-to-commit traceability: commits that omit the slug are not counted.

## H-protocol-adherence — observable

FAQ: "Will my agent actually follow the protocol, or will it just ignore the files?"

Claim: Work is actually carried through the protocol to an evidenced, decision-stamped close rather than abandoned mid-loop.

| Signal | Value | Source |
|---|---|---|
| Done plans: approved | 2 count | metrics |
| Done plans: weak approval | 0 count | metrics |
| Done plans: rejected | 0 count | metrics |
| Done plans: blocked | 0 count | metrics |
| Done plans: approval not recorded | 110 count | metrics |

- Done plans: approved: Done plans whose evidence note records an explicit approved decision.
- Done plans: weak approval: Done plans whose evidence note records a weak approval.
- Done plans: rejected: Done plans whose evidence note records a rejected decision.
- Done plans: blocked: Done plans whose evidence note records a blocked decision.
- Done plans: approval not recorded: Done plans with no machine-readable approval decision in their note. An honesty gap, not evidence of adherence.

## H-resume-time — unavailable

FAQ: "How much time does this actually save me vs. just winging it?"

Claim: Sessions "resume in under a minute instead of fifteen."

| Signal | Value | Source |
|---|---|---|
| Wall-clock session resume time | unavailable | unavailable |

- Wall-clock session resume time: Resume wall-clock is not observable from committed artifacts. Measure it directly under the A/B protocol; do not impute it from commit gaps.

## H-context-reexplanation — unavailable

FAQ: "Is this just going to slow me down? I'm used to vibing."

Claim: Session two does not start with "OK so where were we..." — lower re-explanation cost.

| Signal | Value | Source |
|---|---|---|
| Human-typed context to resume | unavailable | unavailable |

- Human-typed context to resume: Requires the optional usage ledger (plan 114), which is not present in this repo.

## H-cost — unavailable

FAQ: "Does this reduce token usage / cost?"

Claim: Often, indirectly — but this is not benchmarked yet.

| Signal | Value | Source |
|---|---|---|
| Token / cost per work unit | unavailable | unavailable |

- Token / cost per work unit: Requires the optional usage ledger (plan 114), which is not present in this repo.

## What this cannot prove

- No causation: every signal is observational. A correlation between a scaffold artifact and an outcome does not show the scaffold caused the outcome — there is no counterfactual in committed history.
- Snapshot, not time-series: signals are computed from the current working tree and committed history at one moment, not from an event log of how the repo evolved.
- Indirect proxies: amendment counts, evidence completeness, and plan-referencing commits are proxies for the underlying claims, not direct measurements of them.
- Under-counting: plan-referencing commits only match when a commit subject names a known plan slug; commits that omit the slug are missed, so the figure is a floor, not a total.
- Unclean baseline: the methodology has no unscaffolded control in this repo, so "with scaffold" cannot be compared against "without scaffold" here. That comparison is the job of the A/B protocol, which is not run.
- Unavailable is not zero: a signal with source "unavailable" means the tool cannot compute it from committed artifacts (for example, it needs the optional usage ledger from plan 114), not that the underlying value is zero.
